#set($sp="\u0020")
/** 
 * ${description}
 *
 * @author  $sp${USER}
 * @version #if (${version} && ${version} != "") ${version}
#else 1.0.0 
            #end
 * @since   #if (${version} && ${version} != "") ${version}
#else 1.0.0 
            #end
 */